/* let defaultSortParamName = 'order';
let catalogSortItems = document.querySelectorAll(".sort__item");

if (catalogSortItems !== null) {
    catalogSortItems.forEach(item => {
        item.addEventListener('click', function () {
            let itemSortParamValue = item.getAttribute('data-sort-param');
            window.history.replaceState(null, null, '?' + defaultSortParamName + '=' + itemSortParamValue);
          

           
        })
    })
} */